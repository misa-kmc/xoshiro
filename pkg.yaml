# random lib, version: 1.0.0
# authors: genshen <genshenchu@gmail.com>, USTB; zhaorunchu, USTB

version: 2

args:

pkg: "github.com/misa-kmc/xoshiro"
target: "xoshiro"

dependencies:
  packages:
    github.com/google/googletest: {version: release-1.10.0, target: GTest}
